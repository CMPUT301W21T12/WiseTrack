<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.faanggang.wisetrack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WiseTrack">

        <activity android:name=".publish.PublishExperimentActivity4"/>
        <activity android:name=".publish.PublishExperimentActivity3" />
        <activity android:name=".publish.PublishExperimentActivity2" />
        <activity android:name=".publish.PublishExperimentActivity" />
        <activity android:name=".search.SearchActivity" />
        <activity android:name=".MainMenuActivity"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>